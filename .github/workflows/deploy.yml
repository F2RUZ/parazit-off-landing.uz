name: Deploy Parazit Off Project

on:
  push:
    branches: ["main", "master"] # Faqat asosiy branchlar uchun

permissions:
  contents: write
  packages: write
  actions: read

jobs:
  deploy:
    runs-on: [self-hosted, Linux, X64]
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: 📂 Repo-ni ko'chirib olish
        uses: actions/checkout@v4

      - name: 🟢 Node.js o'rnatish
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Kutubxonalarni o'rnatish
        # npm ci o'rniga install ishlatamiz, bu xatolarni oldini oladi
        run: npm install --legacy-peer-deps

      - name: 🏗️ Loyihani Build qilish
        run: npm run build

      - name: 🚀 Serverga Deploy qilish
        run: |
          TARGET_DIR="/home/user/Projects/Euphoria/lands/parazit-off.uz"
          mkdir -p $TARGET_DIR
          
          # Qaysi papka build bo'lganini aniqlash
          if [ -d "out" ]; then
            SOURCE_DIR="out"
          elif [ -d ".next" ]; then
            SOURCE_DIR=".next"
          else
            echo "❌ Build natijasi topilmadi!"
            exit 1
          fi
          
          echo "Deploying from $SOURCE_DIR to $TARGET_DIR..."
          rm -rf $TARGET_DIR/*
          cp -r $SOURCE_DIR/* $TARGET_DIR/
          echo "✅ Muvaffaqiyatli yakunlandi!"
